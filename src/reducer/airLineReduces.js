import {airlines} from '../data/airlines';

const initialStore={airlines}

const airLineReducer=(state,action)=>{
    switch (action.type) {
        default:
            return state;
    }

}
export {initialStore}
export default airLineReducer